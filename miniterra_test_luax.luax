require'pp'
import'miniterra'
print'hello'

local int = 'int!'
local bool = 'bool!'

struct S {

	x: int,
	y: bool,

}

terra g :: {int, int} -> bool

terra f(a: bool, b: int) : {int, bool}

	var i: int, j: bool = 5, true

end

a = 2 + 3 / 5 ^ 7

pr(S)
pr(g)
pr(f)
